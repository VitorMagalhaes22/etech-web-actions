name: Execute e2e tests
on:
  pull_request:
    types: [ "opened", "reopened", "synchronize" ]
    # schedule:
    # - cron: "*/5 * * * *"
  workflow_dispatch:
jobs:
  mvn-run:
    uses: VitorMagalhaes22/workflow-calls/.github/workflows/java-e2e-testes.yaml@main
    secrets:
      git_token: ${{ secrets.TOKEN }}