name: Hello world
on:
  push:
    branches:
      - "master"
  workflow_dispatch:
  schedule:
    - cron: "* * * * *"
jobs:
  hello-world:
    runs-on: ubuntu-latest

    steps:
      - name: Escrever hello world no terminal
        run: echo "Hello world!"